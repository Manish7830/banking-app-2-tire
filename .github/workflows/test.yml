name : Test Workflow

on:
  push:
    baranches:
      - main

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name : Echo Message
        run : echo "Github Action is working successfully ✅"